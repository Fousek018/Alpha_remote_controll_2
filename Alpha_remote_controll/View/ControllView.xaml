<UserControl x:Class="Alpha_remote_controll.View.ControllView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Alpha_remote_controll.View"      
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:xf="clr-namespace:XamlFlair;assembly=XamlFlair.WPF"
             xmlns:view="clr-namespace:Alpha_remote_controll.View"
             xmlns:VM="clr-namespace:Alpha_remote_controll.VM"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="30">
        <materialDesign:Card Style="{StaticResource MaterialDesignOutlinedCard}" VerticalAlignment="Top" Height="60" UniformCornerRadius="15">
            <materialDesign:Card.Effect>
                <DropShadowEffect Color="Black" 
                              ShadowDepth="3" 
                              BlurRadius="20" 
                              Opacity="0.3"/>
            </materialDesign:Card.Effect>
        </materialDesign:Card>
        <TabControl ItemsSource="{Binding Devices}" Margin="0 5 0 0">
            <TabControl.Resources>
                <Style TargetType="{x:Type TabControl}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TabControl}">
                                <Grid KeyboardNavigation.TabNavigation="Local">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <!-- ScrollViewer kolem hlaviček záložek -->
                                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Grid.Row="0" >
                                        <TabPanel x:Name="HeaderPanel"
                                                  Panel.ZIndex="1"
                                                  IsItemsHost="True"
                                                  KeyboardNavigation.TabIndex="1"
                                                  Margin="2,0,0,0" />
                                    </ScrollViewer>
                                    <!-- Obsah jednotlivých záložek -->
                                    <ContentPresenter x:Name="PART_SelectedContentHost"
                                  Grid.Row="1"
                                  Margin="{TemplateBinding Padding}"
                                  ContentSource="SelectedContent" />
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <!-- Název záložky - Device Name -->
                    <TextBlock Text="{Binding DeviceName}" />
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <!-- Obsah záložky - DeviceView -->
                    <local:DeviceView />
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</UserControl>
